name: frontend
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.12.0
  http: ^0.13.3
  flutter_icons: ^1.1.0
  #assets:
  #  - assets/logo.png

flutter:
  uses-material-design: true
